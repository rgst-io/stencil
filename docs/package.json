{
	"name": "@rgst-io/stencil-docs",
	"private": true,
	"type": "module",
	"devDependencies": {
		"vitepress": "^1.6.4",
		"vitepress-sidebar": "^1.31.1"
	},
	"scripts": {
		"docs:dev": "vitepress dev",
		"docs:build": "vitepress build && cp logo.png .vitepress/dist && mkdir -p .vitepress/dist/static && cp ../schemas/* .vitepress/dist/static",
		"docs:preview": "vitepress preview"
	}
}
